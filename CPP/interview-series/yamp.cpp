#include <bits/stdc++.h> 
using namespace std;

class Solution {
  public:
    int minimumRooms(int n, vector<int> &start, vector<int> &end) {
        sort(start.begin(), start.end());
    }
};

int main(){
    int t;
    cin >> t;
    while(t--){

    }

    return 0;
}
